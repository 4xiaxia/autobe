interface:
  display_name: "Autobe"
  short_description: "Repo-specific patterns and workflows for autobe"
  default_prompt: "Use the autobe repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true